<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid justify-content-end">
    <%= link_to "Back to list", list_bookmarks_path, class: "btn btn-primary me-2" %>
    <%= link_to "Back to home page", lists_path, class: "btn btn-primary" %>
  </div>
</nav>
<h1>Add a movie to your list</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, collection: Movie.all, prompt: "Select a movie" %>
  <%= f.input :comment, placeholder: "Enter a comment about the movie" %>
  <%= f.submit "Add movie", class: "btn btn-primary" %>
<% end %>
